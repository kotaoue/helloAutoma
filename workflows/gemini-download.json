{
  "id": "gemini-download",
  "name": "Gemini → Markdown Download",
  "icon": "riFlowChart",
  "folderId": null,
  "content": null,
  "connectedTable": null,
  "drawflow": {
    "edges": [
      {
        "id": "edge-1",
        "source": "node-trigger",
        "target": "node-scrape",
        "sourceHandle": "output-1",
        "targetHandle": "input-1"
      },
      {
        "id": "edge-2",
        "source": "node-scrape",
        "target": "node-download",
        "sourceHandle": "output-1",
        "targetHandle": "input-1"
      }
    ],
    "zoom": 1,
    "nodes": [
      {
        "id": "node-trigger",
        "label": "trigger",
        "data": {
          "disableBlock": false,
          "description": "Run while the Gemini conversation page is open",
          "type": "manual",
          "interval": 0,
          "date": "",
          "time": "",
          "shortcut": ""
        },
        "type": "BlockBasic",
        "position": {
          "x": 0,
          "y": 0
        }
      },
      {
        "id": "node-scrape",
        "label": "javascript-code",
        "data": {
          "disableBlock": false,
          "description": "Scrape: shows alert, logs diagnostics, extracts messages",
          "everyNewTab": false,
          "timeout": 30000,
          "preloadScripts": [],
          "code": "function safeNext(data) {\n  if (typeof automaNextBlock === 'function') {\n    automaNextBlock(data);\n  } else {\n    // automaNextBlock not yet available; poll every 200ms up to 5s\n    var attempts = 0;\n    var timer = setInterval(function() {\n      attempts++;\n      if (typeof automaNextBlock === 'function') {\n        clearInterval(timer);\n        automaNextBlock(data);\n      } else if (attempts >= 25) {\n        clearInterval(timer); // give up — block will time out\n        console.error('[Gemini Download] automaNextBlock never became available');\n      }\n    }, 200);\n  }\n}\n\n// Signal that this code is running (shows in browser console AND as an alert)\nalert('[Gemini Download] Scrape block started.\\nURL: ' + location.href + '\\nautomaNextBlock available: ' + (typeof automaNextBlock === 'function'));\n\ntry {\n  var rawTitle = document.title || '';\n  var title = rawTitle.replace(/\\s*[-\\u2013]\\s*Gemini\\s*$/i, '').trim() || 'Gemini Chat';\n\n  var pairs = [];\n\n  // Strategy 1: <user-query> / <model-response> custom elements (Gemini 2024+)\n  var userQueryEls = document.querySelectorAll('user-query');\n  var modelRespEls = document.querySelectorAll('model-response');\n  if (userQueryEls.length > 0 || modelRespEls.length > 0) {\n    var max1 = Math.max(userQueryEls.length, modelRespEls.length);\n    for (var i = 0; i < max1; i++) {\n      if (userQueryEls[i]) pairs.push({ role: 'User',   text: userQueryEls[i].innerText.trim() });\n      if (modelRespEls[i]) pairs.push({ role: 'Gemini', text: modelRespEls[i].innerText.trim() });\n    }\n  }\n\n  // Strategy 2: data-message-author-role attribute\n  if (pairs.length === 0) {\n    var msgEls = document.querySelectorAll('[data-message-author-role]');\n    for (var j = 0; j < msgEls.length; j++) {\n      var el = msgEls[j];\n      var role = el.getAttribute('data-message-author-role') === 'user' ? 'User' : 'Gemini';\n      pairs.push({ role: role, text: el.innerText.trim() });\n    }\n  }\n\n  // Strategy 3: class-based fallback\n  if (pairs.length === 0) {\n    var userEls  = document.querySelectorAll('.query-text, .user-query-text');\n    var modelEls = document.querySelectorAll('.model-response-text, .response-content');\n    var max3 = Math.max(userEls.length, modelEls.length);\n    for (var k = 0; k < max3; k++) {\n      if (userEls[k])  pairs.push({ role: 'User',   text: userEls[k].innerText.trim() });\n      if (modelEls[k]) pairs.push({ role: 'Gemini', text: modelEls[k].innerText.trim() });\n    }\n  }\n\n  var validPairs = pairs.filter(function(p) { return p.text; });\n  var lines = validPairs.map(function(p) { return '**' + p.role + ':**\\n\\n' + p.text; });\n\n  var content = lines.length > 0\n    ? lines.join('\\n\\n---\\n\\n')\n    : '<!-- No conversation messages found. Run this on a conversation page (URL ends with /app/XXXXXX). -->';\n\n  console.log('[Gemini Download] title:', title, '| messages found:', validPairs.length);\n\n  if (typeof automaSetVariable === 'function') {\n    automaSetVariable('title',    title);\n    automaSetVariable('content',  content);\n    automaSetVariable('msgCount', String(validPairs.length));\n  }\n\n  safeNext({ data: 'scraped ' + validPairs.length + ' messages from: ' + title });\n\n} catch (err) {\n  console.error('[Gemini Download] Scrape error:', err);\n  if (typeof automaSetVariable === 'function') {\n    automaSetVariable('title',   'gemini-error');\n    automaSetVariable('content', 'Scrape error: ' + err.message);\n    automaSetVariable('msgCount', '0');\n  }\n  safeNext({ data: 'scrape error: ' + err.message });\n}"
        },
        "type": "BlockBasic",
        "position": {
          "x": 320,
          "y": 0
        }
      },
      {
        "id": "node-download",
        "label": "javascript-code",
        "data": {
          "disableBlock": false,
          "description": "Download: saves Markdown file to PC",
          "everyNewTab": false,
          "timeout": 15000,
          "preloadScripts": [],
          "code": "function safeNext(data) {\n  if (typeof automaNextBlock === 'function') {\n    automaNextBlock(data);\n  } else {\n    // automaNextBlock not yet available; poll every 200ms up to 5s\n    var attempts = 0;\n    var timer = setInterval(function() {\n      attempts++;\n      if (typeof automaNextBlock === 'function') {\n        clearInterval(timer);\n        automaNextBlock(data);\n      } else if (attempts >= 25) {\n        clearInterval(timer); // give up — block will time out\n        console.error('[Gemini Download] automaNextBlock never became available');\n      }\n    }, 200);\n  }\n}\n\ntry {\n  var title   = (typeof automaRefData === 'function' ? automaRefData('variables', 'title')   : null) || 'gemini';\n  var content = (typeof automaRefData === 'function' ? automaRefData('variables', 'content') : null) || '';\n\n  var today    = new Date().toISOString().slice(0, 10);\n  var markdown = '# ' + title + '\\n\\n'\n    + '> Downloaded: ' + today + '  \\n'\n    + '> Source: ' + location.href + '\\n\\n'\n    + '---\\n\\n'\n    + content + '\\n';\n\n  var filename = (title\n    .replace(/[^a-z0-9]/gi, '_')\n    .replace(/_+/g, '_')\n    .replace(/^_|_$/g, '')\n    .toLowerCase() || 'gemini') + '.md';\n\n  var blob = new Blob([markdown], { type: 'text/markdown' });\n  var url  = URL.createObjectURL(blob);\n  var a    = document.createElement('a');\n  a.href     = url;\n  a.download = filename;\n  document.body.appendChild(a);\n  a.click();\n  setTimeout(function() { URL.revokeObjectURL(url); if (a.parentNode) a.parentNode.removeChild(a); }, 1000);\n\n  console.log('[Gemini Download] File saved as:', filename);\n  if (typeof automaSetVariable === 'function') {\n    automaSetVariable('filename', filename);\n  }\n  safeNext({ data: 'saved: ' + filename });\n\n} catch (err) {\n  console.error('[Gemini Download] Download error:', err);\n  safeNext({ data: 'download error: ' + err.message });\n}"
        },
        "type": "BlockBasic",
        "position": {
          "x": 640,
          "y": 0
        }
      }
    ]
  },
  "table": [],
  "dataColumns": [],
  "description": "Scrapes the currently open Gemini conversation and saves it as a Markdown file to your PC.",
  "trigger": null,
  "createdAt": 1743166029000,
  "updatedAt": 1743685200000,
  "isDisabled": false,
  "settings": {
    "blockDelay": 500,
    "saveLog": true,
    "debugMode": true,
    "onError": "continue",
    "executedBlockOnWeb": true,
    "inputAutofill": false,
    "insertDefaultColumn": false,
    "defaultColumnName": "column"
  },
  "globalData": "{}",
  "version": "1.21.x"
}
